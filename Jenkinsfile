pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Testing pipeline without hello.c...'
                sh 'echo "Hello from Jenkins!"'
            }
        }
    }
}
